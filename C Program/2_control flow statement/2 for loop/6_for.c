#include<stdio.h>
void main(){
      int i;
      for(i = 1; i <= 5; i++)
        printf("Hey ");
        printf("Quackk\n");     //only hey print 5times because curlie bracket not in use so only run 1 printf
}